import { Title } from "@angular/platform-browser";

export interface Products {
    id?: string,
    nombre?: string,
    descripcion?: string,
    cantidad?:string,
    precio?:string,
    categoria?:string
};
 
